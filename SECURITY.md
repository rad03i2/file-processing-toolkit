# Security Policy

Please report security concerns privately through GitHub's security reporting features when available rather than a public issue.

The toolkit operates only on paths explicitly supplied by the user. Rename is preview-only unless `--apply` is supplied. Duplicate detection never deletes files. Symbolic links are skipped during scanning. Review rename plans and keep backups for important data.
